<%# locals: (class_names: "", **attributes) -%>

<%= content_tag :span,
  yield,
  class: cn(
    "font-normal text-foreground", 
    class_names
  ),
  role: :link,
  "aria-disabled": true,
  "aria-current": :page,
  **attributes %>
