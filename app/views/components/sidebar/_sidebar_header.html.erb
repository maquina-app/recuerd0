<%# locals: (class_names: "", **attributes) -%>
<div
  class="<%= cn(
    "flex h-[3.5rem] shrink-0 items-center px-4",
    "data-[state=collapsed]:px-2 data-[state=collapsed]:justify-center",
    class_names
  ) %>"
  <%= tag.attributes(attributes) %>
>
  <%= yield %>
</div>
