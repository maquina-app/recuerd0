<%# locals: (class_names: "", **attributes) -%>

<%= content_tag :div,
  yield,
  class: cn(
    "relative inline-block text-left",
    class_names
  ),
  "data-controller": :dropdown,
  **attributes %>
