<%# locals: (class_names: "", **attributes) -%>
<div
  class="<%= cn("rounded-lg border bg-card text-card-foreground shadow-sm", class_names) %>"
  <%= tag.attributes(attributes) %>
>
  <%= yield %>
</div>
