<%# locals: (class_names: "", **attributes) -%>
<div
  data-slot="alert-description"
  class="<%= cn("text-muted-foreground col-start-2 grid justify-items-start gap-1 text-sm [&_p]:leading-relaxed", class_names) %>"
  <%= tag.attributes(attributes) %>
>
  <%= yield %>
</div>
