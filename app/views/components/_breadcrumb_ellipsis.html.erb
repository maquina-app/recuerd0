<%# locals: (class_names: "", **attributes) -%>
<span
  role="presentation"
  aria-hidden="true"
  class="<%= cn("flex h-9 w-9 items-center justify-center", class_names) %>"
  <%= tag.attributes(attributes) %>
>
  <%= lucide_icon :more_horizontal, size: "w-4 h-4" %>
  <span class="sr-only">More</span>
</span>
