<%# locals: () %>
<% content_for :page_title, "Archived Workspaces" %>

<% content_for :breadcrumb do %>
  <%= breadcrumbs(
    { "Home" => root_path, "Workspaces" => workspaces_path },
    "Archived Workspaces",
  ) %>
<% end %>

<div class="mx-auto max-w-7xl">
  <div class="mb-8">
    <h1 class="text-3xl font-bold">Archived Workspaces</h1>
    <p class="text-muted-foreground mt-2">Manage your archived workspaces</p>
  </div>

  <%= render "components/alert", variant: :info, css_classes: "mb-6" do %>
    <div class="flex items-start gap-3">
      <%= icon_for(:archive, class: "w-4 h-4 flex-shrink-0 text-blue-600 dark:text-blue-400") %>
      <div>
        <strong>About Archived Workspaces</strong>
        <p class="mt-1">Archived workspaces are read-only and cannot be modified. You can restore them at any time to continue working with them.</p>
      </div>
    </div>
  <% end %>

  <% if @workspaces.any? %>
    <div class="space-y-4">
      <% @workspaces.each do |workspace| %>
        <%= render "workspaces/workspace", workspace: workspace %>
      <% end %>
    </div>

    <% if @pagy && @pagy.pages > 1 %>
      <div class="mt-8">
        <%= render "application/components/pagination", pagy: @pagy %>
      </div>
    <% end %>
  <% else %>
    <%= render "components/empty", variant: :dashed do %>
      <%= render "components/empty/header" do %>
        <%= render "components/empty/media", icon: :archive %>
        <%= render "components/empty/title", text: "No archived workspaces" %>
        <%= render "components/empty/description", text: "You haven't archived any workspaces yet." %>
      <% end %>
      <%= render "components/empty/content" do %>
        <%= link_to workspaces_path, data: { component: "button", variant: "secondary" } do %>
          <%= icon_for(:arrow_left, class: "w-4 h-4") %>
          Back to Workspaces
        <% end %>
      <% end %>
    <% end %>
  <% end %>
</div>
