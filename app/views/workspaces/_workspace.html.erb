<%# locals: (workspace:, show_status: false) -%>
<div
  id="<%= dom_id workspace %>"
  class="
    group relative flex items-start gap-4 rounded-lg border border-gray-200 bg-white
    p-6 transition-all hover:shadow-sm dark:border-gray-800 dark:bg-gray-900
    <%= 'opacity-60' if workspace.deleted? || workspace.archived? %>
  "
>
  <div class="flex-1 min-w-0">
    <div class="flex items-start justify-between gap-4">
      <div class="flex-1">
        <h3 class="text-lg font-semibold text-gray-900 dark:text-white">
          <% if workspace.active? %>
            <%= link_to workspace.name,
            workspace_path(workspace),
            class: "hover:text-indigo-600 dark:hover:text-indigo-400" %>
          <% else %>
            <%= workspace.name %>
          <% end %>
        </h3>
        <% if workspace.description.present? %>
          <p class="mt-1 text-sm text-gray-600 dark:text-gray-400">
            <%= workspace.description %>
          </p>
        <% end %>
      </div>

      <div class="flex items-center gap-1">
        <% if workspace.active? %>
          <%= link_to edit_workspace_path(workspace),
              class: "btn-ghost btn-icon-sm",
              title: "Edit workspace" do %>
            <%= lucide_icon :edit, size: "w-4 h-4" %>
          <% end %>

          <%= button_to archive_workspace_path(workspace),
              method: :post,
              class: "btn-ghost btn-icon-sm",
              title: "Archive workspace",
              data: { turbo_confirm: "Are you sure you want to archive this workspace?" } do %>
            <%= lucide_icon :archive, size: "w-4 h-4" %>
          <% end %>

          <%= button_to workspace_path(workspace),
              method: :delete,
              class: "btn-ghost btn-icon-sm text-destructive hover:text-destructive",
              title: "Delete workspace",
              data: { turbo_confirm: "Are you sure you want to delete this workspace?" } do %>
            <%= lucide_icon :trash_2, size: "w-4 h-4" %>
          <% end %>
        <% elsif workspace.archived? %>
          <%= button_to archive_workspace_path(workspace),
              method: :delete,
              class: "btn-ghost btn-icon-sm",
              title: "Unarchive workspace" do %>
            <%= lucide_icon :archive_restore, size: "w-4 h-4" %>
          <% end %>

          <%= button_to workspace_path(workspace),
              method: :delete,
              class: "btn-ghost btn-icon-sm text-destructive hover:text-destructive",
              title: "Delete workspace",
              data: { turbo_confirm: "Are you sure you want to delete this workspace?" } do %>
            <%= lucide_icon :trash_2, size: "w-4 h-4" %>
          <% end %>
        <% elsif workspace.deleted? %>
          <%= button_to restore_workspace_path(workspace),
              method: :post,
              class: "btn-ghost btn-icon-sm",
              title: "Restore workspace" do %>
            <%= lucide_icon :rotate_ccw, size: "w-4 h-4" %>
          <% end %>

          <%= button_to workspace_path(workspace),
              method: :delete,
              class: "btn-ghost btn-icon-sm text-destructive hover:text-destructive",
              title: "Permanently delete workspace",
              data: { turbo_confirm: "Are you sure you want to permanently delete this workspace? This action cannot be undone." } do %>
            <%= lucide_icon :trash_2, size: "w-4 h-4" %>
          <% end %>
        <% end %>
      </div>
    </div>

    <div
      class="
        mt-3 flex items-center gap-4 text-sm text-gray-500 dark:text-gray-400
      "
    >
      <div class="flex items-center gap-1">
        <%= lucide_icon :file_text, size: "w-4 h-4" %>
        <span><%= pluralize(workspace.memories_count, "memory", "memories") %></span>
      </div>
      <div class="flex items-center gap-1">
        <%= lucide_icon :clock, size: "w-4 h-4" %>
        <span>Created
          <%= time_ago_in_words(workspace.created_at) %>
          ago</span>
      </div>
      <% if show_status && (workspace.deleted? || workspace.archived?) %>
        <% if workspace.deleted? %>
          <span
            class="
              inline-flex items-center rounded-full bg-red-100 px-2 py-0.5 text-xs font-medium
              text-red-800 dark:bg-red-900 dark:text-red-200
            "
          >
            Deleted
          </span>
        <% elsif workspace.archived? %>
          <span
            class="
              inline-flex items-center rounded-full bg-yellow-100 px-2 py-0.5 text-xs
              font-medium text-yellow-800 dark:bg-yellow-900 dark:text-yellow-200
            "
          >
            Archived
          </span>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
